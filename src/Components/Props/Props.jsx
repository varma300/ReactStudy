function Props(prps){
    return (
       <p> <i>{prps.value}</i> passed from home to Props using property passing technic</p> // recevid prperty as argument from Home page
    )
}

export default Props